#include <iostream>
using namespace std;

int main()
{
    long long vk, jk, vg, jg, vh, dh, jh;
    cin >> vk >> jk >> vg >> jg >> vh >> dh >> jh;
    cout << (vk * jk + vg * jg) * (vh * dh * jh);
}